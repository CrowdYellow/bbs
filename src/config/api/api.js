const url = 'http://api.laravel.test/api/';

const api = {
  category: url+'categories',
  captcha: url+'captcha',
  register: url+'users/register',
  login: url+'users/login',
};

export default api;
